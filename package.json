{
  "name": "trusted-activations",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "@sendgrid/mail": "^7.7.0",
    "@twilio/runtime-handler": "1.3.0",
    "twilio": "^3.56"
  },
  "devDependencies": {
    "twilio-run": "^3.5.2"
  },
  "engines": {
    "node": "16"
  }
}
